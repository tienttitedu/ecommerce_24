<div class="span9">
      <div class="well well-small">
      <h4><%= t "latest_products" %><small class="pull-right"></small></h4>
    </div>
        <ul class="thumbnails">
        <% @products.each do |product| %>
          <%= render "product_row", product: product, order_item: @order_item %>
        <% end %>
        </ul>
    </div>
